.wrap
  .move-text
    .breaking_news
      h2 Tin nóng
    .marquee
      .sub-marquee(ng-repeat="hotTopic in ctrl.hotTopics")
        a.breaking(href='/#/hottopic/{{hotTopic._id}}') >>{{hotTopic.name}}
      .clearfix
    .clearfix
    //- script.
    //-   $('.marquee').marquee({ pauseOnHover: true, duration: 15000 });
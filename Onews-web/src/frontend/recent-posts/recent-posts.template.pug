.list_vertical
  section.accordation_menu
    div
      label#item2(for='label-2')
        i#i2.icon-leaf
        | Tin gần nhất 
        i.icon-plus-sign.i-right1
        i.icon-minus-sign.i-right2
      #a2.content
        #style-2.scrollbar
          .force-overflow
            .popular-post-grids(ng-repeat="article in ctrl.articles")
              .popular-post-grid
                .post-img
                  a(ui-sref=".article({id: article.id})")
                    img(src='{{article.img}}', alt='')
                .post-text
                  a.pp-title(ui-sref=".article({id: article.id})")  {{article.title}}
                  p
                    | {{article.created_time_string}}
                    a.span_link(href='#')
                      span.glyphicon.glyphicon-comment
                      | 3
                    a.span_link(href='#')
                      span.glyphicon.glyphicon-eye-open
                      | 56
                .clearfix
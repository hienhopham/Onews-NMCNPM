onews-header
hot-topics
div.main-container
  .wrap
    .col-md-8.content-left
      div(ui-view)
        .articles(ng-repeat="category in categories")
          list-articles(category="category", type="main", limit='5')
      some-articles
  .col-md-4.side-bar
    login(ng-if='!user')
    recent-posts
    tags
  .clearfix
onews-footer